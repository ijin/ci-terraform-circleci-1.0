dependencies:
  pre:
    - wget https://dl.bintray.com/mitchellh/terraform/terraform_0.5.1_linux_amd64.zip -O t.zip
    - unzip t.zip -d t
    - pwd
    - ls
    - t/terraform version
  cache_directories:
    - t/terraform_0.5.1
test:
  override:
    - ls
    - t/terraform help
